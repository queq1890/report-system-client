// TODO : redirect to different pages depending on user's role
import React, { PureComponent } from 'react';

class Mypage extends PureComponent {
  render() {
    return (
      <div>Provisional Mypage</div>
    );
  }
}

export default Mypage;
